<header>
    <nav class="navbar navbar-expand-lg navbar-light navbar-backgroundColor" style="background-color: #52057b;">
        <div class="container-fluid">
            <a class="navbar-brand textColorChange" href="/?page=1">Shopping</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse"
                    data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent"
                    aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarSupportedContent">
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item" style="margin-left:20px;">
                        <a class="nav-link textColorChange <%= path === '/' ? 'textColorChange_active' : '' %>"
                           aria-current="page" href="/?page=1">Store</a>
                    </li>
                    <% if (isAuthenticated){ %>
                        <li class="nav-item" style="margin-left:20px;">
                            <a class="nav-link textColorChange <%= path === '/admin/add-product' ? 'textColorChange_active' : '' %>"
                               href="/admin/add-product">Add Product</a>
                        </li>

                        <li class="nav-item" style="margin-left:20px;">
                            <a class="nav-link textColorChange <%= path === '/admin/products' ? 'textColorChange_active' : '' %>"
                               href="/admin/products">Admin Products</a>
                        </li>
                        <li class="nav-item" style="margin-left:20px;">
                            <a class="nav-link textColorChange <%= path === '/orders' ? 'textColorChange_active' : '' %>"
                               href="/orders">Orders</a>
                        </li>

                    <% } else { %>

                    <% } %>

                    <!--                        <li class="nav-item" style="margin-left:20px;">-->
                    <!--                            <a class="nav-link textColorChange <%= path === '/products' ? 'textColorChange_active' : '' %>"-->
                    <!--                               href="/products">Products</a>-->
                    <!--                        </li>-->

                </ul>
                <div class="d-flex">
                    <% if (!isAuthenticated){ %>

                        <li class="nav-item" style="list-style: none;">
                            <a class="nav-link textColorChange <%= path === '/login' ? 'textColorChange_active' : '' %>"
                               href="/login">Login</a>
                        </li>
                        <li class="nav-item" style="list-style: none;">
                            <a class="nav-link textColorChange <%= path === '/register' ? 'textColorChange_active' : '' %>"
                               href="/register">Register</a>
                        </li>
                    <% } else { %>

                        <li class="nav-item" style="list-style: none;">
                            <a class="nav-link textColorChange <%= path === '/cart' ? 'textColorChange_active' : '' %>"
                               href="/cart">Cart</a>
                        </li>
                        <li class="nav-item" style="list-style: none;">
                            <form action="/logout" method="POST">
                                <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                                <button class="btn" style="
        color: #ffffff;
        background-color: #52057b;
        box-shadow: none;
        " type="submit">
                                    Logout
                                </button>

                            </form>

                        </li>

                    <% } %>
                </div>
            </div>
        </div>
    </nav>
    <!--    <nav class="navbar navbar-expand-lg navbar-light "  >-->
    <!--        <div class="container-fluid">-->
    <!--            <a class="navbar-brand textColorChange" href="/?page=1">Shopping</a>-->
    <!--          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"-->
    <!--                  aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">-->
    <!--            <span class="navbar-toggler-icon"></span>-->
    <!--          </button>-->
    <!--          <div class="collapse navbar-collapse" id="navbarNav">-->


    <!--          </div>-->
    <!--        </div>-->
    <!--    </nav>-->

</header>
